document.querySelector("[aria-label='User Settings']").click();
Array.from(document.querySelectorAll('div')).find(el => el.textContent === 'Log Out').click();
Array.from(document.querySelectorAll('div.contents-18-Yxp')).find(el => el.textContent === 'Log Out').click();
